# Plan: Assess the impact of removing the `summary` folder

## Goal
Determine if the `./summary` directory can be removed without impacting the functionality of the `yt-summarizer` codebase.

## Phase 1: Initial Understanding (Exploration)
- Search for references to `summary` or `CACHE_DIR` in the codebase.
- Identify which scripts and functions interact with the `summary` directory.
- Understand whether it stores essential data (like processed transcripts/summaries) or transient data (cache).
- Check if the directory is automatically recreated if missing.

## Phase 2: Design (Assessment)
- Analyze the dependency of the core pipeline on the `summary` directory.
- Determine the consequences of removing it (e.g., re-processing everything, loss of history, error if not recreated).

## Phase 3: Review
- Verify findings against the user's question.

## Phase 4: Final Plan
- (To be completed after exploration)

## Phase 5: Call plan_exit
- Once the assessment is complete and a clear answer is formulated.
